apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssl-storage.fullname" . }}-test"
  labels:
    {{- include "ssl-storage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssl-storage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
